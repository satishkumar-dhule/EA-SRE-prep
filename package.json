{
  "name": "interview-prep-site",
  "version": "1.0.0",
  "description": "Visual Interview Prep Static Site Generator",
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "build": "node scripts/generate-site.js && node scripts/build-site.js",
    "generate": "node scripts/generate-site.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^11.1.0"
  }
}
